# Render — Go services (Spec §24.3)

**The blueprint now lives at the repository root: [`../../render.yaml`](../../render.yaml).**

Render looks for `render.yaml` at the root of the repo by default. It was
previously in this directory, where Render's Blueprint detection would not
find it without extra configuration.

